[pytest]
pythonpath = .
testpaths = tests
addopts = -q
markers =
    unit: pruebas unitarias sin dependencias externas (sin BD ni red)
    integration: pruebas que requieren base de datos u otros servicios
